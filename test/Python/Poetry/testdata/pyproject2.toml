# missing dependencies

[tool.poetry]
description = "test_description"
name = "test_name"
version = "test_version"

[tool.poetry.build]
generate-setup-file = false

[build-system]
build-backend = "test_build_backend"
requires = ["poetry-core>=1.0.0"]
